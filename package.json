{
  "name": "pharma-ecommerce",
  "private": true,
  "scripts": {
    "bootstrap": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run bootstrap && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
